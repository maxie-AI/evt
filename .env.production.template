# Production Environment Variables Template
# Copy these variables to your Vercel project settings

# Supabase Configuration (REQUIRED)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Frontend Environment Variables (REQUIRED for Vite)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# JWT Configuration (REQUIRED)
JWT_SECRET=your-production-jwt-secret-key-make-it-strong
JWT_EXPIRES_IN=7d

# Server Configuration
PORT=3001
NODE_ENV=production

# CORS Configuration (UPDATE with your domain)
CLIENT_URL=https://your-app-domain.vercel.app

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# File Upload
MAX_FILE_SIZE=50000000
UPLOAD_DIR=/tmp/uploads

# Video Processing
MAX_VIDEO_DURATION=3600
TEMP_DIR=/tmp

# OpenAI Configuration (REQUIRED for transcription)
OPENAI_API_KEY=your-openai-api-key

# Instructions:
# 1. Replace all placeholder values with your actual keys
# 2. Add these to your Vercel project environment variables
# 3. Make sure JWT_SECRET is a strong, unique key for production
# 4. Update CLIENT_URL with your actual Vercel domain